import Invitation from "@/components/invitation";

/**
 * Vista de muestra: la invitación tal cual la verá un invitado, pero sin
 * nombre y con el RSVP desconectado. Sirve para revisar y para compartir el
 * enlace general del evento.
 */
export default function Home() {
  return <Invitation />;
}
